name: Run tests

on:
  push:
    branches:
      - main

jobs:
  run_tests:
    runs-on: native

    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: nix flake check
        run: |
          nix flake check --show-trace
